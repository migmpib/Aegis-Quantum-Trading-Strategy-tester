# Руководство по интеграции AI-агента через MCP в систему оркестрации

Данный документ описывает архитектуру и технические шаги для интеграции AI-агента в существующую self-hosted систему оркестрации задач и управления серверной инфраструктурой.

Цель интеграции: создание автономного агента мониторинга, который способен самостоятельно устранять мелкие сбои в работе сервисов (Docker, процессы) и предлагать изменения в кодовой базе для улучшения стабильности, требующие явного подтверждения оператором.

---

## 1. Архитектура решения

Интеграция строится на протоколе **Model Context Protocol (MCP)**. В вашей инфраструктуре необходимо развернуть **MCP-сервер**, который станет шлюзом между AI-моделью (клиентом MCP) и вашей системой (Docker, логи, файловая система).

### Компоненты системы:

1.  **AI-Agent (MCP Client):** Ваша существующая модель, запускаемая по расписанию оркестратором.
2.  **Custom MCP Server:** Node.js или Python приложение, запущенное в вашей инфраструктуре с правами доступа к Docker socket и логам.
3.  **Task Orchestrator:** Инициатор проверки (Cron/Event-based), который вызывает агента с промптом "Проверить здоровье системы".
4.  **Approval Queue (Чат/Интерфейс):** Система для одобрения предложенных изменений кода.

---

## 2. Спецификация MCP-сервера

Вашим разработчикам необходимо реализовать MCP-сервер, предоставляющий следующие примитивы.

### А. Ресурсы (Resources) — Чтение данных

Агент должен иметь доступ к контексту в режиме реального времени.

1.  **URI: `system://logs/{service_name}`**
    *   **Описание:** Возвращает последние N строк логов указанного сервиса.
    *   **Пример использования:** Агент читает `system://logs/backend-api`, чтобы найти stack trace ошибки.

2.  **URI: `system://docker/stats`**
    *   **Описание:** Возвращает JSON со статусом контейнеров (CPU, RAM, Uptime, Healthcheck status).
    *   **Цель:** Выявление зависших или потребляющих много ресурсов контейнеров.

3.  **URI: `project://source/{path}`**
    *   **Описание:** Доступ к исходному коду проекта для анализа причин сбоев.

### Б. Инструменты (Tools) — Активные действия

Инструменты делятся на **Автономные** (безопасные) и **Требующие подтверждения** (критичные).

#### Категория 1: Автономные (Server Management)
*Разрешено выполнение без подтверждения человека, если не затрагивает доступность хоста.*

*   `docker_restart_container(container_id: string)`: Перезапуск конкретного контейнера.
*   `kill_process(pid: number, signal: string)`: Завершение зависшего процесса (с проверкой, что это не системный процесс или SSH).
*   `clear_logs(service_name: string)`: Ротация/очистка логов при переполнении диска.
*   **Важно:** В реализации инструмента добавить "Safety Check" — запрет на остановку критических контейнеров (например, `nginx-ingress`, база данных, сам MCP-сервер).

#### Категория 2: Требующие подтверждения (Code & Critical Ops)
*Любое изменение кода или конфигурации файлов.*

*   `propose_code_change(filepath: string, diff: string, reason: string)`
    *   **Логика:** Не изменяет файл сразу. Создает "патч" или временный файл и отправляет уведомление в ваш чат/систему задач.
    *   **Статус:** Pending Approval.

*   `apply_approved_change(change_id: string)`
    *   **Логика:** Вызывается только после того, как вы в чате нажали "Принять". Применяет патч к реальному файлу.

---

## 3. Сценарий работы (Workflow)

### Шаг 1: Инициализация (Orchestration Trigger)
Ваш оркестратор раз в N часов (или по событию "Alert") запускает сессию с AI-агентом.
**System Prompt для агента:**
> "Ты — инженер по надежности (SRE). Твоя задача — проанализировать состояние системы через доступные MCP-ресурсы.
> 1. Проверь логи и метрики Docker.
> 2. Если есть критические ошибки в рантайме (зависание, утечка памяти) — исправь их автономно, используя безопасные инструменты.
> 3. Если ошибка системная (баг в коде) — проанализируй код и предложи патч через `propose_code_change`.
> 4. НЕ ПРИМЕНЯЙ изменения кода самостоятельно."

### Шаг 2: Мониторинг и Автономное исправление
Агент читает `system://docker/stats`. Видит, что контейнер `worker-01` потребляет 99% CPU и не отвечает.
*   **Действие:** Вызывает `docker_restart_container('worker-01')`.
*   **Результат:** Сервис восстановлен. Агент пишет в отчет: "Перезапустил зависший воркер".

### Шаг 3: Анализ первопричины и Предложение кода
Агент читает `system://logs/worker-01` и видит повторяющуюся ошибку `NullPointerException` в файле `process_data.py`.
*   **Действие:** Читает файл `project://source/process_data.py`. Находит ошибку.
*   **Действие:** Вызывает `propose_code_change`:
    *   `filepath`: `src/process_data.py`
    *   `diff`: (исправленный код с проверкой на None)
    *   `reason`: "Добавлена проверка на null, чтобы избежать падения воркера при пустых входных данных."

### Шаг 4: Уведомление пользователя
Система оркестрации получает результат вызова `propose_code_change`.
*   В ваш чат (Telegram/Slack/Custom Interface) приходит сообщение:
    > 🤖 **AI Proposal:**
    > Обнаружен баг в `process_data.py`, вызывающий падение воркера.
    > **Предлагаемое изменение:** Добавить `if data is None: return`.
    > [Принять] | [Отклонить]

### Шаг 5: Применение (Human-in-the-loop)
Вы нажимаете [Принять].
*   Система вызывает инструмент `apply_approved_change`.
*   Патч применяется к кодовой базе.
*   (Опционально) Запускается CI/CD пайплайн для деплоя.

---

## 4. Технические требования к реализации

Для разработчиков:

1.  **Безопасность (Sandboxing):**
    *   MCP-сервер должен запускаться от отдельного пользователя с ограниченными правами (не root, но в группе docker).
    *   Реализовать "белый список" путей (whitelist), к которым агент имеет доступ на чтение/запись.

2.  **State Management:**
    *   Необходимо хранилище (Redis/DB) для статусов предложенных изменений (`pending`, `approved`, `rejected`), чтобы агент "помнил" свои предложения между сессиями или чтобы система могла их применить асинхронно.

3.  **Logging:**
    *   Все действия агента (вызовы инструментов) должны логироваться в отдельный аудит-лог для разбора инцидентов ("почему агент перезагрузил сервер?").

## 5. Резюме

Внедрение AI через MCP позволит вам:
1.  **Снять рутину:** Автоматический рестарт упавших сервисов и очистка логов.
2.  **Ускорить фикс багов:** Получать готовые Diff-ы кода для проблем, которые уже проявили себя в логах.
3.  **Сохранить контроль:** Ни одна строчка кода не меняется без вашего "ОК", критическая инфраструктура защищена от случайных действий агента.
